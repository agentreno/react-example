import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <App topMessage='hello one' bottomMessage='hello two' />,
  document.getElementById('root')
);
